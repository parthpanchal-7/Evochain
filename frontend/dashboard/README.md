# EvoChain Dashboard

React + Vite frontend for demoing the EvoChain architecture.

## Local Run

```powershell
Set-Location s:\code\Evochain\Evochain\frontend\dashboard
npm install
npm run dev
```

Create a local `.env` from `.env.example` when you want to point the dashboard at a non-default AI API URL.

